# Синерджи Гелиус

Интерактивный дашборд экосистемы ИИ-агентов Корпорации «Синергия».

**Сайт: https://stepansky0012.github.io/helius-synergy/**

Данные — стратегия проекта «ИИ-агенты в Корпорации «Синергия»» от 03.09.2026.

---

## Как читается карта

Полярная матрица с двумя независимыми осями. Ни один канал не дублирует другой.

| Канал | Что кодирует |
|---|---|
| **Радиус** | стадия зрелости: Идея → Реестр → Прототип → Пилот L1 → Промышленная L2 → Автономная L3 |
| **Сектор** | продуктовая линейка; ширина сектора пропорциональна числу агентов в нём |
| **Форма мона** | линейка (тот же ключ, что у сектора) |
| **Обод** | пунктирный, если в стратегии не описаны проблема, действие и эффект |
| **Красный** | открытый долг или блокер; единственное красное на карте |

Матрица вращается как одно жёсткое целое — оборот за 220 секунд. Угол кодирует
линейку, поэтому агент не вправе уезжать из своего сектора: секторы, спицы и
подписи поворачиваются тем же углом. Наведение курсора останавливает вращение.

Клик по агенту открывает **анатомию** — радиальную схему из шести слотов,
одинаковых для всех агентов: роль, эффект, линейка, стадия, ответственный,
системы. Второй режим страницы — целевые показатели до 2028 года, воронка
внедрения из семи этапов и ограничения.

На телефоне борта становятся полосами чипов, вращение выключается (пальцем в
движущуюся цель не попасть), а выноски анатомии заменяются списком.

---

## Как завести своего агента

Сценарий начинается наведением на Солнце и заканчивается заявкой на внешней
орбите. Три шага.

**Стартовый стол.** Слева — сцена: ваш сектор на карте, траектория выведения
и будущий агент пунктиром на орбите Идея. Карта повёрнута к вашему сектору,
как глобус поворачивают к себе: сам сектор, его ширина и порядок соседей те же,
что на большой карте. От агента расходится веер из шести точек — это и есть
бриф, и клик по нему открывает следующий шаг.

Справа три поля. **Роль** подтягивается из корпоративного профиля.
**Сектор** выбирается чипом — и закрывает сразу три слота из шести: линейку,
системы этой линейки и стадию, которая у новой заявки всегда одна.
**Название необязательно** — его предложат на брифовании по вашим же ответам.

Под полями список полезной нагрузки: шесть слотов и напротив каждого сказано,
откуда он берётся, — «из корпоративного профиля», «из выбора сектора»,
«соберём брифом». Ничего не зажигается само по себе.

**Брифование.** Дерево требований. Ветки — те же шесть слотов, которыми на
карте описан любой агент, поэтому второй грамматики учить не нужно: заполнить
слоты нового и прочитать слоты работающего — один и тот же жест.

Система всегда держит подсвеченным следующий блокирующий вопрос, но порядок
не навязан: любая ветка открыта, «про это потом» разрешено. Вопросы двух
родов и различимы формой — каркас слота спросят обязательно, уточняющие
дорастают из уже полученных ответов.

Материалы принимаются в любой момент: вставьте кусок стенограммы или
перетащите текстовый файл — часть листьев закроется сама, и такой ответ
помечен как предложение, которое надо подтвердить.

Подача заперта, пока открыты блокирующие вопросы. Приоритет при этом не
оценивается экспертно, а вычисляется: вопрос из пустого слота блокирует,
остальные — нет.

**Заявка принята.** Агент занимает место на карте: садится на внешнюю орбиту
**Идея** в секторе своего создателя. Обод пунктирный, пока не описаны проблема,
действие и эффект, — по тому же правилу, что и у агентов из стратегии.
Заявки живут в `localStorage` браузера: они видны только вам и никуда не
уходят.

Разбор материалов идёт наивно, по ключевым словам, прямо в браузере. Настоящий
разбор, порождение вопросов моделью и поиск повторяющихся паттернов между
заявками — работа серверной части, которой пока нет; в этих местах стоит
честная заглушка, а не правдоподобные цифры.

---

## Состав

| Файл | Назначение |
|---|---|
| `index.html` | вся страница: разметка, стили, логика, данные. Без сборки и серверной части |
| `Гелиус_агенты.xlsx` | источник данных об агентах — то, что правится руками |
| `sync.py` | переносит таблицу в страницу, проверяет её и публикует |
| `sync.cmd` | запуск `sync.py --deploy` под Windows |

Единственная внешняя зависимость страницы — шрифты с `fonts.googleapis.com`
(Unbounded, Golos Text, JetBrains Mono). Всё остальное внутри файла.

---

## Как менять состав агентов

Агенты живут в `Гелиус_агенты.xlsx`, а не в коде. Блок `var A = [...]` внутри
`index.html` генерируется из таблицы и помечен маркерами
`АГЕНТЫ · НАЧАЛО` / `АГЕНТЫ · КОНЕЦ` — править его руками бессмысленно,
правки затрутся при следующей синхронизации.

1. Открыть `Гелиус_агенты.xlsx`, лист **«Агенты»**.
2. Столбцы `D–J` — свободный текст. `Категория` и `Стадия` — только значения
   из выпадающего списка (лист **«Справочник»**). Столбец `id` — ключ
   сопоставления строк, менять его не нужно.
3. **Добавили строку — на карте появится агент. Удалили строку — исчезнет.**
   Новой строке нужен новый короткий `id` латиницей.
4. Запустить `sync.cmd`.

Сухая проверка без записи:

```
python sync.py --check
```

Перенести правки, но не публиковать:

```
python sync.py
```

Перенести и опубликовать:

```
python sync.py --deploy
```

Синхронизация отказывается работать при дублирующемся или неправильном `id`,
неизвестной категории или стадии, пустом названии — с указанием номера строки.
При отказе ни страница, ни сайт не меняются.

Требуется `python` с `openpyxl` и `node` в `PATH`. Категории и стадии скрипт
читает из самой страницы, поэтому переименование категории синхронизацию
не ломает.

### Локальная раскладка

`sync.py --deploy` ожидает, что рядом с рабочей папкой лежит клон этого
репозитория:

```
...\helius-synergy\      рабочая папка: index.html, таблица, скрипты
...\helius-pages\        клон этого репозитория, из него публикуется сайт
```

В свежем клоне второй папки нет, и `--deploy` честно об этом скажет. Для
своей раскладки поправьте путь в `deploy()`.

---

## Версии

| Тег | Что в ней |
|---|---|
| `v1` | страница на 26 агентах, опубликована на GitHub Pages |
| `v2` | вращение матрицы как жёсткого целого; состав агентов управляется таблицей Excel; убраны дублирующие пояснения |
| `v3` | сценарий заведения агента: от наведения на Солнце до заявки на новой внешней орбите «Идея». Радиусы прежних колец ужаты на 0,87, чтобы она поместилась; периоды обращения при этом не изменились |
| `v3.1` | стартовый стол снова сцена, а не форма: сектор создателя, траектория и агент на орбите идей. Роль подтягивается из профиля, название необязательно и предлагается по ответам, у каждого слота написано, откуда он взялся. В брифовании восстановлена дуга контура — та же, в которую влетаешь со стартового стола |
| `v3.2` | кольцо стартового стола несёт путь агента: бриф, сборка, согласования, развёртывание, работа. Сцена собрана по артборду; анатомия получила шапку, статус и легенду; появилось погружение колесом мыши |
| `v3.3` | заявка на орбите «Идея» перестала выглядеть агентом: пунктирный обод и никакого мона. Название системы — «Синерджи Гелиус» |
| `v3.4` | починен возврат: шапка анатомии больше не садилась на стрелку «к экосистеме». Выкручивание колесом снимает по слою за оборот и не срабатывает над прокручиваемыми панелями |
